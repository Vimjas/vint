language: python

notifications:
  email: false

python:
  - "2.7"
  - "3.4"

install:
  - pip install -e .
  - pip install -U -r test-requirements.txt --use-mirror

script:
  - PATH="./bin:$PATH" py.test --cov vint test

after_success:
  coveralls --verbose
